name: Release

on:
  push:
    branches: [main]
    paths:
      - "apps/**"
      - "crates/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - ".github/workflows/release.yml"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: shapeshifter-linux-x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: shapeshifter-windows-x86_64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Install Linux dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libgl1-mesa-dev \
            libfontconfig1-dev \
            libx11-dev \
            libxcursor-dev \
            libxrandr-dev \
            libxi-dev

      - name: Build release binaries
        run: |
          cargo build --release --package shapeshifter-desktop --target ${{ matrix.target }}
          cargo build --release --package shapeshifter-tui --target ${{ matrix.target }}

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/shapeshifter-desktop dist/${{ matrix.artifact }}
          cp target/${{ matrix.target }}/release/shapeshifter-tui dist/${{ matrix.artifact }}-tui
          chmod +x dist/${{ matrix.artifact }} dist/${{ matrix.artifact }}-tui

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/shapeshifter-desktop.exe dist/${{ matrix.artifact }}.exe
          cp target/${{ matrix.target }}/release/shapeshifter-tui.exe dist/${{ matrix.artifact }}-tui.exe

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Read version from Cargo.toml
        id: version
        run: |
          VERSION=$(grep -m1 '^version' Cargo.toml | sed 's/.*"\(.*\)".*/\1/')
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Generate tag
        id: tag
        run: echo "tag=v${{ steps.version.outputs.version }}" >> "$GITHUB_OUTPUT"

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Shapeshifter ${{ steps.tag.outputs.tag }}
          generate_release_notes: true
          files: artifacts/*
